/**
 * dark model
 * @param baseline - baseline darkness impact at full dark
 * @param terrainScale - scaling of terrain factor at full dark
 */
export type DarkModel = {
  baseline: number
  terrainScale: number
}
